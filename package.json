{
  "name": "emsd",
  "version": "0.1.0",
  "private": true,
  "workspaces": ["apps/*", "packages/*"],
  "scripts": {
    "build": "bun run build:core && bun run build:daemon && bun run build:ems && bun run web:build",
    "build:core": "bun run --cwd packages/core build",
    "build:daemon": "bun run --cwd apps/daemon build",
    "build:ems": "bun run --cwd apps/ems build",
    "typecheck": "bun run typecheck:core && bun run typecheck:daemon && bun run typecheck:ems && bun run typecheck:web",
    "typecheck:core": "bun run --cwd packages/core typecheck",
    "typecheck:daemon": "bun run --cwd apps/daemon typecheck",
    "typecheck:ems": "bun run --cwd apps/ems typecheck",
    "typecheck:web": "bun run --cwd apps/web typecheck",
    "lint": "biome check .",
    "format": "biome format --write .",
    "test": "bun run test:core && bun run test:daemon && bun run test:ems",
    "test:core": "bun run --cwd packages/core test",
    "test:daemon": "bun run --cwd apps/daemon test",
    "test:ems": "bun run --cwd apps/ems test",
    "daemon:dev": "bun run --cwd apps/daemon dev",
    "daemon:start": "bun run scripts/start-daemon.ts",
    "daemon:stop": "bun run scripts/stop-daemon.ts",
    "daemon:start:pm2": "pm2 start ecosystem.config.cjs --only emsd",
    "daemon:restart:pm2": "pm2 restart ecosystem.config.cjs --only emsd --update-env",
    "daemon:stop:pm2": "pm2 delete emsd",
    "daemon:logs:pm2": "pm2 logs emsd",
    "ems": "bun run --cwd apps/ems ems --",
    "battery:list": "bun run --cwd apps/ems ems -- battery list",
    "discover": "bun run --cwd apps/ems ems -- discover",
    "web:dev": "bun run --cwd apps/web dev",
    "web:build": "bun run --cwd apps/web build",
    "web:start": "bun run --cwd apps/web start"
  },
  "devDependencies": {
    "@biomejs/biome": "^1.9.4",
    "@types/node": "^22.15.23",
    "@types/react": "^19.1.4",
    "@types/react-dom": "^19.1.5",
    "bun-types": "^1.3.11",
    "pm2": "^5.4.3",
    "typescript": "^5.8.3"
  }
}
