/**
 * Default settings for evaluation scripts.
 * These are script UX defaults, not algorithm defaults.
 */

/** Default days to replay for dynamic price target evaluation. */
export const DEFAULT_DYNAMIC_PRICE_TARGET_EVALUATION_DAYS = 14;

/** Default backup reserve margin for simulation (percent). */
export const DEFAULT_BACKUP_RESERVE_MARGIN_PERCENT = 2;

/** Default days to score for solar prediction. */
export const DEFAULT_SOLAR_SCORE_DAYS = 7;

/** Default number of top smoothing combinations to display. */
export const DEFAULT_SOLAR_SCORE_TOP_COMBINATIONS = 10;
