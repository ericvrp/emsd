{
  "name": "@emsd/ems",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "ems": "bun run src/index.ts",
    "build": "bun build src/index.ts --outdir dist --target bun",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "bun test"
  },
  "dependencies": {
    "@emsd/core": "workspace:*"
  }
}
